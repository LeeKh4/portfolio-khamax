var typed = new Typed('.typing-1', {
    strings: ['Ninja', 'Designer', ],
    smartBackspace: true,
    typeSpeed: 30,
    backSpeed: 30,
    loop: true,
    loopCount: Infinity // Default value
});